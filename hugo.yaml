baseURL: "https://raysuids.dpdns.org/"
title: "Ray's Blog"

# Hugo v0.128+ 的新分页写法
pagination:
  pagerSize: 10

# 用 Hugo Modules 拉 PaperMod（注意：没有 /v4）
module:
  imports:
    - path: github.com/adityatelange/hugo-PaperMod

theme: "PaperMod"

enableRobotsTXT: true

params:
  env: production
  defaultTheme: auto
  homeInfoParams:
    Title: "嗨，我是 Ray"
    Content: "欢迎来到我的博客。"
