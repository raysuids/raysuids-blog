baseURL: "https://raysuids.dpdns.org/"
title: "Ray's Blog"
paginate: 10
theme: "PaperMod"   # 名称写 PaperMod 即可

# 告诉 Hugo：用模块方式引入 PaperMod 主题（v4）
module:
  imports:
    - path: github.com/adityatelange/hugo-PaperMod/v4

enableRobotsTXT: true

params:
  env: production
  defaultTheme: auto
  homeInfoParams:
    Title: "嗨，我是 Ray"
    Content: "欢迎来到我的博客。"
